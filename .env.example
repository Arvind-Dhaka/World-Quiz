# Connection URL for PostgreSQL database (Neon or local)
# Format: postgresql://username:password@host:port/database?sslmode=require
DATABASE_URL=postgresql://postgres:arvind@2004@localhost:5432/world

# Port the local server should run on
PORT=3000
